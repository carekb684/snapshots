class UserData {

  UserData({this.email, this.displayName, this.userName, this.uid, this.photo, this.points});

  String email;
  String displayName;
  String userName;
  String uid;
  String photo;
  int points;
}